# Лог воспроизведения 2025-12-05

## Наблюдения по логам
- Сразу после запуска приложения восстанавливается прогресс по книге `148` и пользовательский тип (`UserType.free`), затем выполняется `setChapters` с `userInitiated=true` и `samePlaylist=false`, что повторно подготавливает плейлист этой книги.
- Автоподготовка повторяется после рестарта процесса: снова грузится книга `148`, вызывается `setChapters` и стартует воспроизведение (`play(): book=148, chapter=0`), при этом других книг в плеер не загружалось.
- При обращении к избранному подтягиваются данные другой книги (`147`) и её главы из кэша, но вызовов `setChapters`/`play` для неё в логах нет — плеер продолжает считаться настроенным на `148`.
- Таймер бесплатных секунд и списания кредитов (`CreditsConsumer`) срабатывает только для книги `148`, что подтверждает отсутствие переключения на другую книгу.

## Вывод
Логи показывают, что при старте сессии плейлист книги `148` автоматически восстанавливается и подготавливается с флагом `userInitiated=true`. За время сессии не было ни одного вызова `setChapters` для другой книги (например, `147`), поэтому нажатие Play на другой карточке не привело к смене источника: плеер продолжает играть восстановленную книгу `148`.
